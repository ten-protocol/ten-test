<?xml version="1.0" encoding="utf-8"?>
<pysystest type="auto">

    <description>
        <title>ERC20 OBX transfer from deployment owner to bridge account, then across the bridge (L1 to L2)</title>
        <purpose><![CDATA[]]>
            Checks that the deployment owner (pre-funded) can transfer tokens to the bridge account on the L1 OBX
            token, and that this is then reflected across into the L2 OBX token. 
        </purpose>
    </description>

    <classification>
        <groups inherit="true">
            <group></group>
        </groups>
        <modes inherit="true">
            <mode>obscuro.dev</mode>
            <mode>obscuro.local</mode>
        </modes>
    </classification>

    <data>
        <class name="PySysTest" module="run"/>
    </data>

    <traceability>
        <requirements>
            <requirement id=""/>
        </requirements>
    </traceability>
</pysystest>
