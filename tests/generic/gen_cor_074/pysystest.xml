<?xml version="1.0" encoding="utf-8"?>
<pysystest type="auto">

    <description>
        <title>Events: javascript client contract.getPastEvents, filter and topics</title>
        <purpose><![CDATA[]]>
Uses filter and topic to extract events of interest as a polling approach using getPastLogs. It filters on an address
field, on a set of integer fields, and on a string field. As a string it seems you cannot filter within web3, but
rather you sha3 the string value and specify it in the topic list as part of the bloom filters.
        </purpose>
    </description>

    <classification>
        <groups inherit="true">
            <group>events</group>
        </groups>
        <modes inherit="true">
            <mode>obscuro.dev</mode>
            <mode>obscuro.local</mode>
            <mode>ropsten</mode>
            <mode>ganache</mode>
        </modes>
    </classification>

    <data>
        <class name="PySysTest" module="run"/>
    </data>

    <traceability>
        <requirements>
            <requirement id=""/>
        </requirements>
    </traceability>
</pysystest>
