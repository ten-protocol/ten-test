<?xml version="1.0" encoding="utf-8"?>
<pysystest type="auto">

    <description>
        <title>Callbacks: two phase commit where the callback triggers a require</title>
        <purpose><![CDATA[
Deploys the error contract which implements the two phase commit approach. Transacting with the contract results in a
callback to a method which has a require that triggers - we expect this to be handled in the same way in which it
would had it been done without the call back approach.
]]>
        </purpose>
    </description>

    <classification>
        <groups inherit="true">
            <group>callbacks</group>
        </groups>
        <modes inherit="true">
            <mode>ten.sepolia</mode>
            <mode>ten.uat</mode>
            <mode>ten.dev</mode>
            <mode>ten.local</mode>
            <mode>ten.sim</mode>
        </modes>
    </classification>

    <data>
        <class name="PySysTest" module="run"/>
    </data>

    <traceability>
        <requirements>
            <requirement id=""/>
        </requirements>
    </traceability>
</pysystest>
