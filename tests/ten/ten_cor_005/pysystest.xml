<?xml version="1.0" encoding="utf-8"?>
<pysystest type="auto">

    <description>
        <title>Event relevancy - event with a non-indexed address received by all subscriptions through own WE</title>
        <purpose><![CDATA[
In this test four accounts are used, each subscribing to all events from the Relevancy contract. The accounts are;

  Account4 - own wallet
  Account1 - own wallet
  Account2 - own wallet
  Account3 - own wallet

The event NonIndexedAddressAndNumber(address addr1, uint256 value) is used with the address from account4 in the
event fields. The test ensures that all accounts see the event as it is a non indexed address field.
]]>
        </purpose>
    </description>

    <classification>
        <groups inherit="true">
            <group>events</group>
            <group>web3js</group>
        </groups>
        <modes inherit="true">
            <mode>obscuro.sepolia</mode>
            <mode>obscuro.uat</mode>
            <mode>obscuro.dev</mode>
            <mode>obscuro.local</mode>
        </modes>
    </classification>

    <data>
        <class name="PySysTest" module="run"/>
    </data>

    <traceability>
        <requirements>
            <requirement id=""/>
        </requirements>
    </traceability>
</pysystest>
